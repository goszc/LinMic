cmake_minimum_required(VERSION 3.16)
project(linmic_native_tests LANGUAGES CXX)
enable_testing()
find_package(Threads REQUIRED)
add_executable(linmic-native-test test.cpp)
target_compile_features(linmic-native-test PRIVATE cxx_std_17)
target_compile_options(linmic-native-test PRIVATE -Wall -Wextra -Werror)
target_link_libraries(linmic-native-test PRIVATE Threads::Threads)
add_test(NAME native-wire-and-ring COMMAND linmic-native-test)
